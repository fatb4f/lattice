{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'd=$PWD; while [ \"$d\" != / ]; do p=$d/.kg/hooks/codex/user-prompt-submit; [ -f \"$p\" ] && exec sh \"$p\"; d=${d%/*}; done; exit 0'",
            "timeout": 10,
            "statusMessage": "Selecting KG context"
          },
          {
            "type": "command",
            "command": "sh -c 'd=$PWD; while [ \"$d\" != / ]; do p=$d/.kg/codex/tools/drift-hook; [ -f \"$p\" ] && exec sh \"$p\"; d=${d%/*}; done; exit 0'",
            "timeout": 15,
            "statusMessage": "Checking Codex drift KG"
          }
        ]
      }
    ],
    "TurnEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'd=$PWD; while [ \"$d\" != / ]; do p=$d/.kg/codex/tools/drift-hook; [ -f \"$p\" ] && exec sh \"$p\"; d=${d%/*}; done; exit 0'",
            "timeout": 30,
            "statusMessage": "Checking Codex drift KG"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'd=$PWD; while [ \"$d\" != / ]; do p=$d/.kg/codex/tools/drift-hook; [ -f \"$p\" ] && exec sh \"$p\"; d=${d%/*}; done; exit 0'",
            "timeout": 30,
            "statusMessage": "Checking Codex drift KG"
          }
        ]
      }
    ]
  }
}
